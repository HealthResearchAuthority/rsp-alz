name: featureflags-update
trigger: none
pr:
  branches:
    include:
      - main
  paths:
    include:
      - '5.spoke-network/main.featureflags-update.bicep'
      - '5.spoke-network/featureflags-parameters/**'
      - '5.spoke-network/modules/featureflags-update/**'

parameters:
  - name: env
    displayName: Environment
    type: string
    default: dev
    values:
      - dev
      - systemtest_manual
      - systemtest_auto
      - uat
      - pre_prod
      - production
  - name: location
    displayName: 'Deployment location'
    type: string
    default: uksouth

stages:
  - stage: PRValidation
    displayName: 'PR Validation Feature Flags Update'
    condition: eq(variables['Build.Reason'], 'PullRequest')
    jobs:
      - job: lint
        displayName: 'Lint Feature Flags Templates'
        pool:
          name: rsp-manageddevopspool
        steps:
          - checkout: self
          - script: |
              echo "Installing Azure CLI..."
              curl -sL https://aka.ms/InstallAzureCLIDeb | sudo bash
            displayName: 'Install Azure CLI'
          - bash: |
              az bicep install
              az bicep version
            displayName: 'Install Bicep CLI'
          - bash: az bicep build --file ./5.spoke-network/main.featureflags-update.bicep
            displayName: 'Build Bicep Template'

      - job: validate
        displayName: 'Validate Feature Flags Changes'
        dependsOn: lint
        condition: succeeded()
        pool:
          name: rsp-manageddevopspool
        variables:
          - group: ${{ parameters.env }}
        steps:
          - checkout: self
          - script: |
              echo "Installing Azure CLI..."
              curl -sL https://aka.ms/InstallAzureCLIDeb | sudo bash
            displayName: 'Install Azure CLI'
          - bash: |
              az bicep install
              az bicep version
            displayName: 'Install Bicep CLI'
          - task: AzureCLI@2
            displayName: 'Validate Deployment'
            inputs:
              azureSubscription: $(${{ parameters.env }})
              scriptType: bash
              scriptLocation: inlineScript
              failOnStandardError: true
              inlineScript: |
                az config set bicep.use_binary_from_path=false --only-show-errors
                az deployment sub validate --name "FeatureFlags-$(Build.BuildId)" \
                  --location $(location) \
                  --template-file ./5.spoke-network/main.featureflags-update.bicep \
                  --parameters ./5.spoke-network/featureflags-parameters/${{ parameters.env }}.parameters.bicepparam \
                  --parameters parSharedServicesSubscriptionId=$(subscriptionId)
      - job: preview
        displayName: 'Preview Feature Flags Changes'
        dependsOn: validate
        condition: succeeded()
        pool:
          name: rsp-manageddevopspool
        variables:
          - group: ${{ parameters.env }}
        steps:
          - checkout: self
          - script: |
              echo "Installing Azure CLI..."
              curl -sL https://aka.ms/InstallAzureCLIDeb | sudo bash
            displayName: 'Install Azure CLI'
          - bash: |
              az bicep install
              az bicep version
            displayName: 'Install Bicep CLI'
          - task: AzureCLI@2
            displayName: 'What-If Feature Flags Update'
            inputs:
              azureSubscription: $(${{ parameters.env }})
              scriptType: bash
              scriptLocation: inlineScript
              failOnStandardError: true
              inlineScript: |
                az config set bicep.use_binary_from_path=false --only-show-errors
                az deployment sub what-if --location $(location) --template-file ./5.spoke-network/main.featureflags-update.bicep \
                  --parameters ./5.spoke-network/featureflags-parameters/${{ parameters.env }}.parameters.bicepparam \
                  --parameters parSharedServicesSubscriptionId=$(subscriptionId)

  - stage: ManualDeployment
    displayName: 'Manual Deployment of Feature Flags'
    dependsOn: []
    condition: and(succeeded(), ne(variables['Build.Reason'], 'PullRequest'))
    variables:
      location: ${{ parameters.location }}
    jobs:
      - deployment: deploy
        displayName: 'Deploy Feature Flags Changes'
        pool:
          name: rsp-manageddevopspool
        environment: 'alz-featureflags-update'
        variables:
          - group: ${{ parameters.env }}
        strategy:
          runOnce:
            deploy:
              steps:
                - checkout: self
                - script: |
                    echo "Installing Azure CLI..."
                    curl -sL https://aka.ms/InstallAzureCLIDeb | sudo bash
                  displayName: 'Install Azure CLI'
                - bash: |
                    az bicep install
                    az bicep version
                  displayName: 'Install Bicep CLI'
                - task: AzureCLI@2
                  displayName: 'Deploy Feature Flags Update'
                  inputs:
                    azureSubscription: $(${{ parameters.env }})
                    scriptType: bash
                    scriptLocation: inlineScript
                    failOnStandardError: true
                    inlineScript: |
                      az config set bicep.use_binary_from_path=false --only-show-errors
                      az deployment sub create --name "FeatureFlags-Deploy-$(Build.BuildId)" \
                        --location $(location) \
                        --template-file ./5.spoke-network/main.featureflags-update.bicep \
                        --parameters ./5.spoke-network/featureflags-parameters/${{ parameters.env }}.parameters.bicepparam \
                        --parameters parSharedServicesSubscriptionId=$(subscriptionId)
